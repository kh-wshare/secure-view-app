# Copy to .env and adjust for your environment. Expo inlines EXPO_PUBLIC_*
# vars at build time, so the app must be restarted (not just reloaded) after
# changing this file. Defaults match the Postman collection's local environment.
EXPO_PUBLIC_API_BASE_URL=http://localhost:8080
EXPO_PUBLIC_GATEWAY_URL=http://localhost:8081
